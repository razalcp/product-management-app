# Services

This directory contains the core business logic of the application.
Services process data, apply business rules, and orchestrate the flow of information. They receive data from controllers, perform necessary operations, and interact with the data access layer (repositories) to fetch or persist data.
This separation keeps the controllers thin and makes the business logic easily testable and reusable.
