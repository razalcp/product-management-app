# Routes

This directory contains the route definitions for the Express application.
Routes define the API endpoints (URLs) and the HTTP methods (GET, POST, PUT, DELETE) they support. Each route is mapped to a specific controller function that will handle the request.
Route files help in organizing the API structure logically (e.g., `productRoutes.js`, `userRoutes.js`).
