# Models

This directory contains the database schemas and models.
For this application, it holds the Mongoose schemas that define the structure of the data stored in MongoDB (e.g., User, Product).
Models provide the interface for interacting with the database collections.
