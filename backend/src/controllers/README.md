# Controllers

This directory contains the controller functions for the application.
Controllers are responsible for handling incoming HTTP requests, validating input, invoking the appropriate service methods to perform business logic, and sending the HTTP response.
They act as the entry point for API requests but do not contain heavy business logic themselves.
