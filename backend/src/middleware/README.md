# Middleware

This directory contains custom Express middleware functions.
Middleware functions are executed during the request-response cycle. They can modify the request or response objects, end the request-response cycle, or call the next middleware function in the stack.
Common examples include authentication middleware (verifying JWT tokens), error handling, and request logging.
